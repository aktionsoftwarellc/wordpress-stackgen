<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <title>Stack criada</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container mt-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card shadow">
            <div class="card-body text-center">
              <h2 class="mb-4">Stack criada com sucesso!</h2>
              <div class="alert alert-success">
                <p class="mb-1">Pasta: <b><%= pasta %></b></p>
                <p class="mb-1">Comando para rodar:</p>
                <code><%= comando %></code>
              </div>
              <% if (typeof msg !== 'undefined' && msg) { %>
              <div><%- msg %></div>
              <% } %>
              <form method="post" action="/deploy">
                <input type="hidden" name="pasta" value="<%= pasta %>" />
                <button type="submit" class="btn btn-success mt-3">
                  Deploy (docker-compose up -d)
                </button>
              </form>
              <a href="/" class="btn btn-primary mt-3">Voltar</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
